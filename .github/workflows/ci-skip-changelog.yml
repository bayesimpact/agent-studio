name: CI

on:
  pull_request:
    branches:
      - main
    paths:
      - CHANGELOG.md

jobs:
  tests:
    if: startsWith(github.head_ref, 'chore/changelog-v')
    runs-on: ubuntu-latest
    steps:
      - run: echo "Changelog-only PR — skipping tests"