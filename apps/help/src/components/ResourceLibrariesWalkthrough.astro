---
/**
 * Resource libraries walkthrough — v2. Mounts the static React island built from the REAL
 * imported `@caseai-connect/ui` components, with the page assembly transcribed from
 * `apps/web` and labels auto-synced from the app locales (see `src/walkthroughs/` and
 * `apps/help/CLAUDE.md` → "Walkthrough generation v2"). Lightweight, no live app.
 * `.wt-scope` product tokens come from `global.css` (imported by BaseLayout).
 * Pass `lang="fr"` for the French copy.
 */
import ResourceLibrariesWalkthroughIsland from "@/walkthroughs/ResourceLibrariesScene.tsx"
type Lang = "en" | "fr"
const { lang = "en" } = Astro.props as { lang?: Lang }
---

<ResourceLibrariesWalkthroughIsland client:only="react" lang={lang} />
